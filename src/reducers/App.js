const defaultState = false
const showAddPost = (state = defaultState, action) => {
  const { showPost, type } = action

  switch (type) {
    default:
      return state
  }
}

export default showAddPost
